# Dipendenze Python del progetto.
# Le librerie verranno aggiunte progressivamente con l'avanzare dei task.

# --- T0: Setup ---
Flask

# --- T1: Database ---
# Driver MySQL per Python come da dispensa "Configurazione e uso di un database
# MySQL con Python" (Programmazione 2), che utilizza il modulo pymysql.
PyMySQL

# --- T7: Dashboard statistiche ---
# Grafici generati lato server come da dispensa "Rappresentazione grafica
# dei dati" (Programmazione 2).
matplotlib

# --- T8: Sicurezza (HTTPS/TLS) ---
# Usata da genera_certificato.py per creare il certificato autofirmato
# (chiave RSA + firma SHA-256, algoritmi trattati in Cybersecurity).
cryptography
